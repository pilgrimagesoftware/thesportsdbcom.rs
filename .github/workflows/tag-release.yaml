name: Tag Release

on:
  pull_request:
    types: [closed]
    branches:
      - master

permissions:
  contents: write

jobs:
  tag-release:
    uses: pilgrimagesoftware/github-actions/.github/workflows/rust-tag-release.yaml@master
    secrets: inherit
